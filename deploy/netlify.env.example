# Netlify → Site configuration → Environment variables
# Scope: "All" or "Production" — must be available at BUILD time

# REQUIRED — your Render API URL (must end with /api)
VITE_API_URL=https://media-downloader-api.onrender.com/api

# Netlify sets these automatically (do not add manually):
# NETLIFY=true
# CONTEXT=production